name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install formula
        run: brew install --formula Formula/git-brunch.rb
      - name: Test formula
        run: brew test --formula Formula/git-brunch.rb
